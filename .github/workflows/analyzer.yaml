name: Powershell Script Analyzer
on: push

jobs:
  package-analysis:
    if: "contains(github.event.head_commit.message, 'pwsh')"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: PS-Check
        uses: SleepySysadmin/github-action-pscheck@v0.4.2
